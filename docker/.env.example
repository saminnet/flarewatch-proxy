# FlareWatch Proxy (Docker Compose)
# Copy to `services/proxy/docker/.env` and edit.

PORT=3000

# Optional: Set a custom location identifier
# FLAREWATCH_PROXY_LOCATION=NYC

# Optional: Enable authentication (recommended for production)
# Generate with: `openssl rand -base64 32`
# FLAREWATCH_PROXY_TOKEN=<proxy-token>
